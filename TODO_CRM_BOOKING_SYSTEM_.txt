# 📋 TODO:  Развитие системы бронирования

## 🎯 Общая концепция

Создание полноценной CRM-системы бронирования, состоящей из двух связанных ботов:
1. **bronivik_jr** (существующий) — бронирование оборудования на целый день
2. **bronivik_crm** (новый) — бронирование кабинетов на часы с синхронизацией доступности оборудования

---

# 📌 ЧАСТЬ 1: Доработки существующего бота (bronivik_jr)

## 1. 1 API Endpoint для проверки доступности оборудования

### 1.1.1 Создание HTTP-сервера
- [x] **Создать файл `internal/api/server.go`**
  - [x] Инициализация HTTP-сервера (gin/chi/стандартный net/http)
  - [x] Настройка middleware для логирования
  - [x] Настройка CORS (если нужно)
  - [x] Graceful shutdown

- [x] **Создать файл `internal/api/handlers. go`**
  - [x] Handler `GET /api/v1/availability/{item_name}? date={date}`
    - Проверка доступности аппарата по имени и дате
    - Возвращает:  `{"available": true/false, "booked_count": N, "total":  M}`
  - [x] Handler `GET /api/v1/availability/bulk`
    - Массовая проверка доступности
    - Body: `{"items": ["item1", "item2"], "dates": ["2025-12-22", "2025-12-23"]}`
  - [x] Handler `GET /api/v1/items`
    - Список всех аппаратов с их total_quantity

### 1.1.2 Авторизация API
- [x] **Создать файл `internal/api/auth.go`**
  - [x] Middleware для проверки API-ключа в заголовке `X-API-Key`
  - [x] Хранение API-ключей в конфиге или базе
  - [x] Rate limiting для защиты от DDoS

- [x] **Обновить `configs/config.yaml`**
  ```yaml
  api: 
    enabled: true
    port: 8081
    api_keys:
      - key: "secret-key-for-crm-bot"
        name: "bronivik_crm"
        permissions:  ["read: availability", "read:items"]
  ```

### 1.1.3 Модификация database
- [x] **Добавить метод в `internal/database/database.go`**
  ```go
  // GetItemAvailabilityByName проверяет доступность по имени аппарата
  func (db *DB) GetItemAvailabilityByName(ctx context.Context, itemName string, date time.Time) (*AvailabilityInfo, error)
  
  // GetItemByName возвращает аппарат по имени
  func (db *DB) GetItemByName(ctx context. Context, name string) (*models.Item, error)
  ```

### 1.1.4 Интеграция в main.go
- [x] **Запуск API-сервера параллельно с ботом**
  ```go
  // В main.go
  if cfg.API.Enabled {
      apiServer := api.NewServer(cfg, db)
      go apiServer.Start()
  }
  ```

---

## 1.2 Рефакторинг синхронизации с Google Sheets

### 1.2.1 Переход на точечное обновление
- [x] **Рефакторинг `internal/google/sheets_simple.go`**
  - [x] Заменить `ReplaceBookingsSheet()` на:
    ```go
    // UpsertBooking обновляет или вставляет одну запись
    func (s *SheetsService) UpsertBooking(booking *models.Booking) error
    
    // DeleteBookingRow удаляет строку бронирования
    func (s *SheetsService) DeleteBookingRow(bookingID int64) error
    
    // UpdateBookingStatus обновляет только статус в строке
    func (s *SheetsService) UpdateBookingStatus(bookingID int64, status string) error
    ```
  
  - [x] Реализовать поиск строки по ID бронирования
    ```go
    // FindBookingRow находит номер строки по ID бронирования
    func (s *SheetsService) FindBookingRow(bookingID int64) (int, error)
    ```

  - [x] Кэширование маппинга `booking_id -> row_number` в Redis/memory

### 1.2.2 Создание Worker для отложенных обновлений
- [x] **Создать файл `internal/worker/sheets_worker.go`**
  ```go
  type SheetsWorker struct {
      queue chan SheetTask
      sheetsService *google.SheetsService
      retryPolicy RetryPolicy
  }
  
  type SheetTask struct {
      Type       string // "upsert", "delete", "update_status"
      BookingID  int64
      Booking    *models. Booking
      Status     string
      RetryCount int
      CreatedAt  time. Time
  }
  
  func (w *SheetsWorker) Start(ctx context.Context)
  func (w *SheetsWorker) EnqueueTask(task SheetTask)
  ```
- [x] **Создать очередь задач**
  - [x] Использовать Redis для персистентной очереди
  - [x] Fallback на in-memory queue
  - [x] Retry logic с exponential backoff
  - [x] Dead letter queue для failed tasks
- [x] **Создать файл `internal/worker/retry. go`**
  ```go
  type RetryPolicy struct {
      MaxRetries     int
      InitialDelay   time.Duration
      MaxDelay       time.Duration
      BackoffFactor  float64
  }
  
  func (r *RetryPolicy) NextDelay(attempt int) time.Duration
  ```

### 1.2.3 Создание таблицы для очереди синхронизации
- [x] **Добавить миграцию в `internal/database/database.go`**
  ```sql
  CREATE TABLE IF NOT EXISTS sync_queue (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      task_type TEXT NOT NULL,
      booking_id INTEGER NOT NULL,
      payload TEXT, -- JSON
      status TEXT DEFAULT 'pending', -- pending, processing, completed, failed
      retry_count INTEGER DEFAULT 0,
      last_error TEXT,
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
      processed_at DATETIME,
      next_retry_at DATETIME
  );
  
  CREATE INDEX IF NOT EXISTS idx_sync_queue_status ON sync_queue(status);
  CREATE INDEX IF NOT EXISTS idx_sync_queue_next_retry ON sync_queue(next_retry_at);
  ```

- [x] **CRUD методы для sync_queue**
  ```go
  func (db *DB) CreateSyncTask(ctx context.Context, task *SyncTask) error
  func (db *DB) GetPendingSyncTasks(ctx context.Context, limit int) ([]SyncTask, error)
  func (db *DB) UpdateSyncTaskStatus(ctx context.Context, id int64, status string, err string) error
  func (db *DB) GetFailedSyncTasks(ctx context.Context) ([]SyncTask, error)
  ```

### 1.2.4 Интеграция воркера
 - [x] **Пример модификации в `internal/bot/utils.go`**
  ```go
  // Было: 
  go func() {
      time.Sleep(1 * time.Second)
      b.SyncBookingsToSheets()
  }()
  
  // Стало:
  b.sheetsWorker.EnqueueTask(SheetTask{
      Type:       "upsert",
      BookingID: booking.ID,
      Booking:    &booking,
  })
 [x] **Проверка доступности слота**
 [x] **Создание бронирования с проверками**
  ```
  - [x] Внедрено: enqueue в finalizeBooking/confirm/reject/reopen/complete вместо прямых вызовов

---

## 1.3 Устранение блокировок и data race

### 1.3.1 Анализ текущих проблем
- [x] **Выявить синхронные вызовы, блокирующие UI**
  - `SyncBookingsToSheets()` — вызывается синхронно
  - `SyncScheduleToSheets()` — вызывается синхронно
  - `AppendBookingToSheets()` — вызывается синхронно

### 1.3.2 Внедрение паттерна "Event Sourcing Light"
- [x] **Создать файл `internal/events/events.go`**
  ```go
  type Event struct {
      ID        int64
      Type      string // booking_created, booking_confirmed, etc.
      Payload   json.RawMessage
      CreatedAt time.Time
      Processed bool
  }
  
  type EventBus struct {
      subscribers map[string][]EventHandler
      mu          sync.RWMutex
  }
  
  type EventHandler func(event Event) error
  ```

- [x] **События для бронирований**
  - `booking_created` — новое бронирование
  - `booking_confirmed` — подтверждено менеджером
  - `booking_cancelled` — отменено
  - `booking_completed` — завершено
  - `booking_item_changed` — изменен аппарат
  - [x] Подписка EventBus -> SheetsWorker (upsert/status), замена прямых вызовов из бота
  - `booking_created` — новое бронирование
  - `booking_confirmed` — подтверждено менеджером
  - `booking_cancelled` — отменено
  - `booking_completed` — завершено
  - `booking_item_changed` — изменен аппарат

### 1.3.3 Защита от data race
- [x] **Использовать транзакции для критических операций**
  ```go
  func (db *DB) CreateBookingWithLock(ctx context. Context, booking *models.Booking) error {
      tx, err := db. BeginTx(ctx, nil)
      if err != nil {
          return err
      }
      defer tx.Rollback()
      
      // 1. Проверка доступности внутри транзакции
      available, err := checkAvailabilityTx(tx, booking.ItemID, booking.Date)
      if err != nil || !available {
          return ErrNotAvailable
      }
      
      // 2. Создание бронирования
      err = createBookingTx(tx, booking)
      if err != nil {
          return err
      }
      
      // 3. Добавление события в очередь
      err = createSyncTaskTx(tx, booking)
      if err != nil {
          return err
      }
      
      return tx.Commit()
  }
  ```

- [x] **Добавить optimistic locking для бронирований**
  ```sql
  ALTER TABLE bookings ADD COLUMN version INTEGER DEFAULT 1;
  ```
  
  ```go
  func (db *DB) UpdateBookingWithVersion(ctx context.Context, booking *models. Booking) error {
      result, err := db.ExecContext(ctx, 
          `UPDATE bookings SET status = ?, version = version + 1, updated_at = ? 
           WHERE id = ?  AND version = ?`,
          booking.Status, time.Now(), booking.ID, booking.Version)
      
      rows, _ := result.RowsAffected()
      if rows == 0 {
          return ErrConcurrentModification
      }
      return nil
  }
  ```

---

## 1.4 Улучшения конфигурации

### 1.4.1 Хранение items в базе данных
- [x] **Создать таблицу items**
  ```sql
  CREATE TABLE IF NOT EXISTS items (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      name TEXT UNIQUE NOT NULL,
      description TEXT,
      total_quantity INTEGER NOT NULL DEFAULT 1,
      sort_order INTEGER NOT NULL DEFAULT 0,
      is_active BOOLEAN DEFAULT 1,
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
      updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
  );
  CREATE INDEX IF NOT EXISTS idx_items_sort ON items(sort_order, id);
  ```

- [x] **CRUD для items**
  ```go
  func (db *DB) GetActiveItems(ctx context. Context) ([]models.Item, error)
  func (db *DB) CreateItem(ctx context.Context, item *models.Item) error
  func (db *DB) UpdateItem(ctx context. Context, item *models.Item) error
  func (db *DB) ReorderItem(ctx context.Context, id int64, newOrder int64) error
  func (db *DB) DeactivateItem(ctx context.Context, id int64) error
  ```

- [x] **Команды бота для управления items (только для менеджеров)**
  - `/add_item <name> <quantity>` — добавить аппарат
  - `/edit_item <name> <new_quantity>` — изменить количество
  - `/list_items` — список аппаратов
  - `/disable_item <name>` — деактивировать
  - `/set_item_order <name> <order>` — установить порядок вывода
  - `/move_item_up <name>` / `/move_item_down <name>` — поменять местами с соседним

---

# 📌 ЧАСТЬ 2: Новый бот (bronivik_crm)

## 2.1 Архитектура нового бота

### 2.1.1 Структура проекта
```
bronivik_crm/
├── cmd/
│   └── bot/
│       └── main.go
├── configs/
│   └── config.yaml
├── internal/
│   ├── api/
│   │   └── client.go          # Клиент для API bronivik_jr
│   ├── bot/
│   │   ├── bot.go
│   │   ├── handler.go
│   │   ├── manager.go
│   │   ├── cabinet_handler.go # Работа с кабинетами
│   │   └── booking_handler.go # Почасовое бронирование
│   ├── database/
│   │   └── database. go
│   ├── models/
│   │   ├── cabinet.go
│   │   ├── cabinet_schedule.go
│   │   ├── hourly_booking.go
│   │   └── user.go
│   ├── google/
│   │   └── sheets.go
│   └── worker/
│       └── sync_worker.go
├── docker/
│   └── docker-compose.yml
├── go.mod
├── go. sum
└── Dockerfile
```

### 2.1.2 Основные сущности

#### Кабинеты (Cabinets)
- [x] **Создать файл `internal/models/cabinet. go`**
  ```go
  package models
  
  import "time"
  
  type Cabinet struct {
      ID          int64     `json:"id"`
      Name        string    `json:"name"`
      Description string    `json:"description"`
      IsActive    bool      `json:"is_active"`
      CreatedAt   time.Time `json:"created_at"`
      UpdatedAt   time. Time `json:"updated_at"`
  }
  ```

#### Расписание кабинетов (Cabinet Schedule)
- [x] **Создать файл `internal/models/cabinet_schedule. go`**
  ```go
  package models
  
  import "time"
  
  type CabinetSchedule struct {
      ID         int64     `json:"id"`
      CabinetID  int64     `json:"cabinet_id"`
      DayOfWeek  int       `json:"day_of_week"` // 0-6 (Sunday-Saturday)
      StartTime  string    `json:"start_time"`  // "09:00"
      EndTime    string    `json:"end_time"`    // "18:00"
      SlotDuration int     `json:"slot_duration"` // в минутах (60)
      IsActive   bool      `json:"is_active"`
      CreatedAt  time.Time `json:"created_at"`
      UpdatedAt  time.Time `json:"updated_at"`
  }
  
  // CabinetScheduleOverride для исключений (праздники, особые дни)
  type CabinetScheduleOverride struct {
      ID         int64     `json:"id"`
      CabinetID  int64     `json:"cabinet_id"`
      Date       time.Time `json:"date"`
      IsClosed   bool      `json:"is_closed"` // полностью закрыт
      StartTime  string    `json:"start_time"`
      EndTime    string    `json:"end_time"`
      Reason     string    `json:"reason"`
      CreatedAt  time.Time `json:"created_at"`
  }
  ```

#### Почасовое бронирование
- [x] **Создать файл `internal/models/hourly_booking.go`**
  ```go
  package models
  
  import "time"
  
  type HourlyBooking struct {
      ID           int64     `json:"id"`
      UserID       int64     `json:"user_id"`
      UserName     string    `json:"user_name"`
      UserNickname string    `json:"user_nickname"`
      Phone        string    `json:"phone"`
      CabinetID    int64     `json:"cabinet_id"`
      CabinetName  string    `json:"cabinet_name"`
      ItemID       int64     `json:"item_id"`      // ID аппарата из bronivik_jr
      ItemName     string    `json:"item_name"`    // Имя аппарата
      Date         time.Time `json:"date"`
      StartTime    string    `json:"start_time"`   // "10:00"
      EndTime      string    `json:"end_time"`     // "11:00"
      Status       string    `json:"status"`       // pending, confirmed, cancelled, completed
      Comment      string    `json:"comment"`
      ClientName   string    `json:"client_name"`  // Имя клиента (для CRM)
      ClientPhone  string    `json:"client_phone"` // Телефон клиента
      CreatedAt    time.Time `json:"created_at"`
      UpdatedAt    time.Time `json:"updated_at"`
  }
  ```

---

## 2.2 База данных CRM бота

### 2.2.1 Миграции
- [x] **Создать `internal/database/database. go`**
  ```go
  func createTables(db *sql.DB) error {
      queries := []string{
          // Пользователи (аналогично bronivik_jr)
          `CREATE TABLE IF NOT EXISTS users (...)`,
          
          // Кабинеты
          `CREATE TABLE IF NOT EXISTS cabinets (
              id INTEGER PRIMARY KEY AUTOINCREMENT,
              name TEXT UNIQUE NOT NULL,
              description TEXT,
              is_active BOOLEAN DEFAULT 1,
              created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
              updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
          )`,
          
          // Расписание кабинетов
          `CREATE TABLE IF NOT EXISTS cabinet_schedules (
              id INTEGER PRIMARY KEY AUTOINCREMENT,
              cabinet_id INTEGER NOT NULL,
              day_of_week INTEGER NOT NULL, -- 0-6
              start_time TEXT NOT NULL,      -- "09:00"
              end_time TEXT NOT NULL,        -- "18:00"
              slot_duration INTEGER DEFAULT 60, -- минуты
              is_active BOOLEAN DEFAULT 1,
              created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
              updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
              FOREIGN KEY (cabinet_id) REFERENCES cabinets(id),
              UNIQUE(cabinet_id, day_of_week)
          )`,
          
          // Исключения расписания
          `CREATE TABLE IF NOT EXISTS cabinet_schedule_overrides (
              id INTEGER PRIMARY KEY AUTOINCREMENT,
              cabinet_id INTEGER NOT NULL,
              date DATE NOT NULL,
              is_closed BOOLEAN DEFAULT 0,
              start_time TEXT,
              end_time TEXT,
              reason TEXT,
              created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
              FOREIGN KEY (cabinet_id) REFERENCES cabinets(id),
              UNIQUE(cabinet_id, date)
          )`,
          
          // Почасовые бронирования
          `CREATE TABLE IF NOT EXISTS hourly_bookings (
              id INTEGER PRIMARY KEY AUTOINCREMENT,
              user_id INTEGER NOT NULL,
              user_name TEXT NOT NULL,
              user_nickname TEXT,
              phone TEXT,
              cabinet_id INTEGER NOT NULL,
              cabinet_name TEXT NOT NULL,
              item_id INTEGER, -- из bronivik_jr
              item_name TEXT,
              date DATE NOT NULL,
              start_time TEXT NOT NULL,
              end_time TEXT NOT NULL,
              status TEXT DEFAULT 'pending',
              comment TEXT,
              client_name TEXT,
              client_phone TEXT,
              created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
              updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
              version INTEGER DEFAULT 1,
              FOREIGN KEY (cabinet_id) REFERENCES cabinets(id)
          )`,
          
          // Очередь синхронизации
          `CREATE TABLE IF NOT EXISTS sync_queue (...)`,
          
          // Индексы
          `CREATE INDEX IF NOT EXISTS idx_hourly_bookings_date ON hourly_bookings(date)`,
          `CREATE INDEX IF NOT EXISTS idx_hourly_bookings_cabinet ON hourly_bookings(cabinet_id)`,
          `CREATE INDEX IF NOT EXISTS idx_hourly_bookings_item ON hourly_bookings(item_id)`,
          `CREATE INDEX IF NOT EXISTS idx_hourly_bookings_status ON hourly_bookings(status)`,
          `CREATE INDEX IF NOT EXISTS idx_hourly_bookings_time ON hourly_bookings(date, start_time, end_time)`,
      }
      // ... 
  }
  ```

### 2.2.2 Методы для работы с кабинетами
- [x] **CRUD кабинетов**
  ```go
  func (db *DB) CreateCabinet(ctx context. Context, cabinet *models.Cabinet) error
  func (db *DB) GetCabinet(ctx context. Context, id int64) (*models.Cabinet, error)
  func (db *DB) GetActiveCabinets(ctx context. Context) ([]models.Cabinet, error)
  func (db *DB) UpdateCabinet(ctx context.Context, cabinet *models.Cabinet) error
  func (db *DB) DeactivateCabinet(ctx context.Context, id int64) error
  ```

### 2.2.3 Методы для работы с расписанием
- [x] **CRUD расписания**
  ```go
  func (db *DB) SetCabinetSchedule(ctx context. Context, schedule *models.CabinetSchedule) error
  func (db *DB) GetCabinetSchedule(ctx context. Context, cabinetID int64) ([]models.CabinetSchedule, error)
  func (db *DB) GetCabinetScheduleForDay(ctx context.Context, cabinetID int64, dayOfWeek int) (*models.CabinetSchedule, error)
  func (db *DB) AddScheduleOverride(ctx context.Context, override *models.CabinetScheduleOverride) error
  func (db *DB) GetScheduleOverrides(ctx context. Context, cabinetID int64, startDate, endDate time.Time) ([]models.CabinetScheduleOverride, error)
  ```

### 2.2.4 Методы для почасового бронирования
- [x] **Проверка доступности слота**
  ```go
  // CheckSlotAvailability проверяет доступность временного слота
  func (db *DB) CheckSlotAvailability(ctx context.Context, cabinetID int64, date time.Time, startTime, endTime string) (bool, error) {
      // 1. Проверяем, попадает ли слот в рабочее время кабинета
      // 2. Проверяем, нет ли override на эту дату
      // 3. Проверяем, нет ли пересечения с существующими бронированиями
  }
  
  // GetAvailableSlots возвращает все свободные слоты на дату
  func (db *DB) GetAvailableSlots(ctx context.Context, cabinetID int64, date time.Time) ([]TimeSlot, error)
  
  // TimeSlot структура временного слота
  type TimeSlot struct {
      StartTime string
      EndTime   string
      Available bool
  }
  ```

- [x] **Создание бронирования с проверками**
  ```go
  func (db *DB) CreateHourlyBookingWithChecks(ctx context.Context, booking *models.HourlyBooking, bronivikClient *api.BronivikClient) error {
      tx, err := db. BeginTx(ctx, nil)
      if err != nil {
          return err
      }
      defer tx.Rollback()
      
      // 1. Проверяем доступность кабинета на время
      available, err := checkSlotAvailabilityTx(tx, booking.CabinetID, booking.Date, booking. StartTime, booking. EndTime)
      if ! available {
          return ErrSlotNotAvailable
      }
      
      // 2. Проверяем доступность аппарата через API bronivik_jr
      if booking.ItemID > 0 {
          itemAvailable, err := bronivikClient.CheckItemAvailability(booking.ItemName, booking.Date)
          if err != nil || !itemAvailable {
              return ErrItemNotAvailable
          }
      }
      
      // 3. Создаем бронирование
      err = createHourlyBookingTx(tx, booking)
      if err != nil {
          return err
      }
      
      return tx.Commit()
  }
  ```

- [x] **Сохранение аппарата и данных клиента в почасовой броне**
  - [x] Добавить поля в `hourly_bookings`: `item_name`, `client_name`, `client_phone`
  - [x] Обновить модель `HourlyBooking` (CRM)
  - [x] Обновить insert/select/scan методы для бронирований
  - [x] Добавить backfill/ALTER TABLE миграцию для существующих баз

- [x] **Валидации и правила бронирования (CRM)**
  - [x] Нельзя бронировать в прошлом; учесть `min_advance_booking`
  - [x] Округление времени по `slot_duration`
  - [x] Проверка пересечений по кабинету (уже есть) + (опционально) лимит активных броней на пользователя
  - [x] Финальная проверка перед подтверждением/созданием (race condition)

---

## 2.3 Клиент API для связи с bronivik_jr

### 2.3.1 Клиент
- [x] **Создать файл `internal/api/client.go`**
  ```go
  package api
  
  import (
      "context"
      "encoding/json"
      "fmt"
      "net/http"
      "time"
  )
  
  type BronivikClient struct {
      baseURL    string
      apiKey     string
      httpClient *http.Client
  }
  
  type AvailabilityResponse struct {
      Available   bool  `json:"available"`
      BookedCount int   `json:"booked_count"`
      Total       int   `json:"total"`
  }
  
  type Item struct {
      ID            int64  `json:"id"`
      Name          string `json:"name"`
      TotalQuantity int64  `json:"total_quantity"`
  }
  
  func NewBronivikClient(baseURL, apiKey string) *BronivikClient {
      return &BronivikClient{
          baseURL:  baseURL,
          apiKey:  apiKey,
          httpClient: &http.Client{
              Timeout: 10 * time.Second,
          },
      }
  }
  
  // CheckItemAvailability проверяет доступность аппарата
  func (c *BronivikClient) CheckItemAvailability(itemName string, date time. Time) (bool, error) {
      url := fmt.Sprintf("%s/api/v1/availability/%s? date=%s", 
          c.baseURL, itemName, date.Format("2006-01-02"))
      
      req, err := http.NewRequest("GET", url, nil)
      if err != nil {
          return false, err
      }
      req.Header.Set("X-API-Key", c.apiKey)
      
      resp, err := c. httpClient.Do(req)
      if err != nil {
          return false, err
      }
      defer resp.Body. Close()
      
      if resp.StatusCode != http.StatusOK {
          return false, fmt.Errorf("API returned status %d", resp.StatusCode)
      }
      
      var result AvailabilityResponse
      if err := json.NewDecoder(resp. Body).Decode(&result); err != nil {
          return false, err
      }
      
      return result.Available, nil
  }
  
  // GetItems получает список всех аппаратов
  func (c *BronivikClient) GetItems() ([]Item, error) {
      // ... 
  }
  
  // BulkCheckAvailability массовая проверка
  func (c *BronivikClient) BulkCheckAvailability(items []string, dates []time.Time) (map[string]map[string]bool, error) {
      // ...
  }
  ```

### 2.3.2 Кэширование результатов
- [x] **Добавить кэширование через Redis**
  ```go
  type CachedBronivikClient struct {
      client      *BronivikClient
      redisClient *redis. Client
      cacheTTL    time.Duration
  }
  
  func (c *CachedBronivikClient) CheckItemAvailability(itemName string, date time.Time) (bool, error) {
      cacheKey := fmt. Sprintf("item_availability:%s:%s", itemName, date.Format("2006-01-02"))
      
      // Проверяем кэш
      cached, err := c.redisClient.Get(ctx, cacheKey).Result()
      if err == nil {
          return cached == "true", nil
      }
      
      // Запрос к API
      available, err := c. client.CheckItemAvailability(itemName, date)
      if err != nil {
          return false, err
      }
      
      // Сохраняем в кэш
      c.redisClient. Set(ctx, cacheKey, fmt.Sprintf("%t", available), c.cacheTTL)
      
      return available, nil
  }
  ```

---

## 2.4 Telegram Bot Handlers

### 2.4.1 Команды пользователя
- [x] **Основные команды**
  - `/start` — начало работы
  - `/book` — создать бронирование кабинета
  - `/my_bookings` — мои бронирования
  - `/cancel_booking [ID]` — отменить бронирование
  - `/help` — справка

### 2.4.2 Команды менеджера
- [x] **Управление кабинетами**
  - `/add_cabinet <name>` — добавить кабинет
  - `/list_cabinets` — список кабинетов
  - `/cabinet_schedule <cabinet_id>` — просмотр расписания
  - `/set_schedule <cabinet_id> <day> <start> <end>` — установить расписание
  - `/close_cabinet <cabinet_id> <date> [reason]` — закрыть кабинет на дату
  
- [x] **Управление бронированиями**
  - `/pending` — ожидающие подтверждения
  - `/approve [ID]` — подтвердить
  - `/reject [ID]` — отклонить
  - `/today_schedule` — расписание на сегодня
  - `/tomorrow_schedule` — расписание на завтра

### 2.4.3 Сценарий бронирования
- [x] **Реализовать flow бронирования**
  ```
  1. Пользователь:  /book
  2. Бот: Выберите кабинет [инлайн клавиатура с кабинетами]
  3. Пользователь: [выбирает кабинет]
  4. Бот: Выберите аппарат (или "Без аппарата") [клавиатура]
     - Бот проверяет доступность аппаратов через API bronivik_jr
     - Показывает только доступные на выбранные даты
  5. Пользователь: [выбирает аппарат]
  6. Бот: Выберите дату [календарь]
  7. Пользователь: [выбирает дату]
  8. Бот:  Выберите время [слоты с учетом доступности кабинета И аппарата]
  9. Пользователь:  [выбирает время]
  10. Бот:  Введите данные клиента (ФИО, телефон)
  11. Бот: Подтверждение бронирования
  12. Уведомление менеджерам
  ```

- [x] **UX и статусы (CRM)**
  - [x] Инлайн-кнопки подтверждения/отклонения для менеджеров
  - [x] Сообщения пользователю при отказе: слот занят / аппарат недоступен
  - [x] Повторный выбор шага (аппарат/дата/время) без перезапуска сценария

- [x] **Пользователи (CRM)**
  - [x] Get-or-create пользователя по `telegram_id` в БД
  - [x] Хранить `username/first_name/last_name/phone` (если доступно)

### 2.4.4 Календарь и выбор времени
- [x] **Создать `internal/bot/calendar.go`**
  ```go
  // GenerateCalendarKeyboard создает инлайн-клавиатуру календаря
  func GenerateCalendarKeyboard(year, month int, availableDates map[string]bool) tgbotapi. InlineKeyboardMarkup
  
  // GenerateTimeSlotsKeyboard создает клавиатуру с временными слотами
  func GenerateTimeSlotsKeyboard(slots []TimeSlot, selectedDate string) tgbotapi.InlineKeyboardMarkup
  ```

---

## 2.5 Конфигурация CRM бота

### 2.5.1 Файл конфигурации
- [x] **Создать `configs/config.yaml`**
  ```yaml
  app:
    name:  "bronivik-crm"
    environment: "staging"
    version: "1.0.0"
  
  telegram:
    bot_token: ${CRM_BOT_TOKEN}
    debug: true
  
  database: 
    path: "./data/crm.db"
    postgres:
      host:  ${POSTGRES_HOST}
      user: ${POSTGRES_USER}
      password: ${POSTGRES_PASSWORD}
      database: "bronivik_crm"
  
  redis:
    host: ${REDIS_HOST}
    port: 6379
    password: ${REDIS_PASSWORD}
    db: 1
  
  bronivik_api:
    base_url: "http://bronivik-jr: 8081"
    api_key: ${BRONIVIK_API_KEY}
    timeout: 10s
    cache_ttl: 5m
  
  google: 
    credentials_file: ${GOOGLE_CREDENTIALS_FILE}
    spreadsheet_id: ${CRM_SPREADSHEET_ID}
  
  managers:
    - 123456789
    - 987654321
  
  booking: 
    default_slot_duration: 60  # минуты
    min_advance_booking: 1     # минимум за 1 час до начала
    max_advance_days: 30       # максимум на 30 дней вперед
    allow_multiple_slots: true # разрешить бронирование нескольких слотов подряд
  ```

---

## 2.6 Docker конфигурация

### 2.6.1 Dockerfile
- [x] **Создать `Dockerfile`**
  ```dockerfile
  FROM golang: 1.21-alpine AS builder
  
  WORKDIR /app
  COPY go.mod go.sum ./
  RUN go mod download
  COPY .  .
  RUN CGO_ENABLED=0 GOOS=linux go build -o /bronivik-crm ./cmd/bot
  
  FROM alpine:latest
  RUN apk --no-cache add ca-certificates tzdata
  WORKDIR /app
  COPY --from=builder /bronivik-crm .
  COPY configs/ ./configs/
  
  CMD ["./bronivik-crm", "--config=configs/config.yaml"]
  ```

### 2.6.2 Docker Compose для всей системы
- [x] **Создать `docker/docker-compose.yml`**
  ```yaml
  version:  '3.8'
  
  services:
    bronivik-jr:
      build: 
        context: ../bronivik_jr
        dockerfile: Dockerfile
      environment:
        - BOT_TOKEN=${JR_BOT_TOKEN}
        - GOOGLE_CREDENTIALS_FILE=/app/credentials.json
      volumes:
        - jr-data:/app/data
        - ./credentials.json:/app/credentials.json:ro
      ports:
        - "8081:8081"  # API endpoint
      networks:
        - bronivik-network
      depends_on:
        - redis
        - postgres
  
    bronivik-crm:
      build: 
        context: ../bronivik_crm
        dockerfile: Dockerfile
      environment:
        - CRM_BOT_TOKEN=${CRM_BOT_TOKEN}
        - BRONIVIK_API_KEY=${BRONIVIK_API_KEY}
        - GOOGLE_CREDENTIALS_FILE=/app/credentials. json
      volumes: 
        - crm-data:/app/data
        - ./credentials.json:/app/credentials.json:ro
      networks:
        - bronivik-network
      depends_on: 
        - bronivik-jr
        - redis
        - postgres
  
    postgres:
      image:  postgres:15-alpine
      environment: 
        - POSTGRES_USER=${POSTGRES_USER}
        - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
        - POSTGRES_DB=bronivik
      volumes:
        - postgres-data:/var/lib/postgresql/data
      networks:
        - bronivik-network
  
    redis: 
      image: redis: 7-alpine
      command: redis-server --appendonly yes
      volumes:
        - redis-data:/data
      networks: 
        - bronivik-network
  
  volumes:
    jr-data:
    crm-data: 
    postgres-data: 
    redis-data:
  
  networks:
    bronivik-network: 
      driver: bridge
  ```

---

# 📌 ЧАСТЬ 3: Миграция и тестирование

## 3.1 Миграции данных

### 3.1.1 Миграция items в базу данных
- [x] **Скрипт миграции `scripts/migrate_items. go`**
  - Чтение items из config.yaml
  - Вставка в таблицу items
  - Проверка уникальности

### 3.1.2 Миграция схемы bronivik_jr
- [x] **Добавление новых полей**
  - `version` для optimistic locking
  - Индексы для оптимизации

### 3.1.3 Миграции схемы bronivik_crm
- [x] **Миграции для `hourly_bookings`**
  - `item_name`, `client_name`, `client_phone`
  - Индексы по `cabinet_id,start_time,end_time,status`

## 3.2 Тестирование

### 3.2.1 Unit тесты
- [x] **Тесты для database**
  - `database_test.go` — тесты CRUD
  - `availability_test.go` — тесты проверки доступности

- [x] **Тесты для API**
  - `api_test.go` — тесты endpoints
  - `auth_test.go` — тесты авторизации

- [x] **Тесты для worker**
  - `worker_test.go` — тесты очереди задач
  - `retry_test.go` — тесты retry логики

### 3.2.2 Integration тесты
- [x] **E2E тесты**
  - Сценарий бронирования от начала до конца
  - Проверка синхронизации с Google Sheets
  - Проверка взаимодействия двух ботов

### 3.2.3 Load тесты
- [x] **Нагрузочное тестирование**
  - Одновременное бронирование одного слота
  - Массовая синхронизация с Google Sheets
  - Проверка rate limiting API

---

# 📌 ЧАСТЬ 4: Мониторинг и логирование

## 4.1 Метрики

### 4.1.1 Prometheus метрики
- [x] **Метрики для bronivik_jr**
  - `bookings_total` — общее количество бронирований
  - `api_requests_total` — количество API запросов
  - `sheets_sync_duration_seconds` — время синхронизации
  - `sheets_sync_errors_total` — ошибки синхронизации

- [x] **Метрики для bronivik_crm**
- [x] **Метрики для bronivik_crm**
  - `hourly_bookings_total`
  - `cabinet_occupancy_rate`
  - `bronivik_api_requests_total`
  - `bronivik_api_latency_seconds`

### 4.1.2 Алерты
- [x] **Настроить алерты**
  - Sheets sync failed > 5 раз подряд
  - API недоступен > 1 минуты
  - База данных недоступна

## 4.2 Логирование

### 4.2.1 Structured logging
- [x] **Внедрить structured logging (zerolog/zap)**
  - Request ID для трейсинга
  - Уровни логирования
  - JSON формат для продакшена

## 4.3 Healthchecks
- [x] **Health endpoints**
  - [x] `GET /healthz` (CRM)
  - [x] `GET /readyz` (CRM) — проверка БД/Redis/API

---

# 📌 ПРИОРИТЕТЫ РЕАЛИЗАЦИИ

## Фаза 1: Критические изменения bronivik_jr (1-2 недели)
1. ✅ API endpoint для проверки доступности
2. ✅ Асинхронная синхронизация с Google Sheets (worker)
3. ✅ Защита от data race (транзакции)

## Фаза 2: Базовый функционал bronivik_crm (2-3 недели)
1. ✅ Структура проекта и модели
2. ✅ Работа с кабинетами и расписанием
3. ✅ Клиент API для bronivik_jr
4. ✅ Базовый flow бронирования

## Фаза 3: Интеграция и UI (1-2 недели)
1. ✅ Календарь и выбор времени
2. ✅ Уведомления менеджерам
3. ✅ Интеграция с Google Sheets

## Фаза 4: Полировка (1 неделя)
1. ✅ Тестирование
2. ✅ Мониторинг
3. ✅ Документация
4. ✅ Деплой

---

# 📝 ЗАМЕТКИ

## Потенциальные проблемы
1. **Синхронизация доступности** — если аппарат забронирован в bronivik_jr после проверки в bronivik_crm, возможен конфликт
   - Решение:  финальная проверка перед подтверждением + уведомление

2. **Google API rate limits** — при большом количестве операций
   - Решение: batching операций + exponential backoff

3. **Отказоустойчивость** — если bronivik_jr недоступен
   - Решение:  кэширование + graceful degradation (разрешать бронирование без аппарата)

## Улучшения на будущее
- [x] Напоминания клиентам о бронировании
- [x] Статистика и аналитика

---

# 📌 ЧАСТЬ 5: Технический долг и улучшения

## 5.1 Критические проблемы (исправить немедленно)

### 5.1.1 Race condition в userStates [DONE]
- [x] **internal/bot/handler.go: userStates map не защищен mutex**
  - Проблема: Одновременный доступ к `map[int64]*UserState` может вызвать panic
  - Решение: Использовать `sync.Map` или добавить `sync.RWMutex`

### 5.1.2 Дубликаты ID в configs/items.yaml [DONE]
- [x] **Исправить дублирующиеся ID элементов**
  - ID 8: УФ СЕРЕБРИСТЫЙ, УФ СИНИЙ
  - ID 14: УФ ЧЕРНЫЙ, УФ ЗЕЛЕНЫЙ, MPT
  - ID 16: Скарлет белый, Скарлет белый мамонтенок
  - ID 46: Accent, Accent Prime
  - Решение: Назначить уникальные ID

### 5.1.3 Потеря состояния пользователя при перезапуске [DONE]
- [x] **Сохранять userStates в Redis/DB**
  - При перезапуске бота все пользователи теряют состояние
  - Решение: Сериализация состояния в Redis с TTL

---

## 5.2 Качество кода (высокий приоритет)

### 5.2.1 Рефакторинг длинных функций
- [x] **internal/bot/handler.go: handleMessage() — 130+ строк** [DONE]
  - Разбит на отдельные обработчики в `user_handlers.go`
- [x] **internal/bot/handler.go: handleCallbackQuery() — 140+ строк** [DONE]
  - Разбит на отдельные обработчики в `callback_handlers.go`
- [x] **internal/bot/manager.go — 800+ строк** [DONE]
  - Разбит на файлы: manager_bookings.go, manager_items.go, manager_stats.go, manager_sync.go
- [x] **internal/database/database.go — 900+ строк** [DONE]
  - Разбит на: users.go, bookings.go, items.go, sync_queue.go

### 5.2.2 Вынести константы [DONE]
- [x] **Создать internal/models/constants.go** (бывший status.go)
- [x] **Вынести магические числа**
  - Размер пагинации
  - Время напоминания (09:00)
  - Redis TTL (24h)
  - Периоды экспорта (1 мес назад, 2 мес вперед)
  - Размер канала воркера (сейчас 1000)

### 5.2.3 Модуляризация бота [DONE]
- [x] **Разбить internal/bot/handler.go на модули**
  - bot.go (структура и запуск)
  - user_handlers.go (обработка сообщений пользователей)
  - callback_handlers.go (обработка инлайн-кнопок)
  - sync.go (синхронизация с Google Sheets)
  - manager.go (логика менеджера)
  - utils.go (вспомогательные функции)

### 5.2.4 Устранить дублирование кода
- [ ] **Пагинация в manager.go**
  - `sendItemsPage`, `sendManagerItemsPage`, `sendScheduleItemsPage` — почти идентичны
  - Решение: Создать generic pagination component
- [ ] **Редактирование страниц**
  - `editItemsPage`, `editScheduleItemsPage`, `editManagerItemsPage`
  - Решение: Извлечь общую логику

---

## 5.3 Производительность (средний приоритет)

### 5.3.1 N+1 запросы
- [x] **internal/database/database.go: GetAvailabilityForPeriod()**
  - Проблема: Вызов GetBookedCount в цикле для каждого дня
  - Решение: Батч-запрос с GROUP BY
  ```go
  func (db *DB) GetBookedCountBatch(ctx context.Context, itemID int64, dates []time.Time) (map[string]int, error)
  ```

### 5.3.2 Кэширование
- [ ] **internal/database/database.go: items cache**
  - Кэш items никогда не инвалидируется
  - Добавить TTL или invalidation при изменении
- [ ] **internal/google/sheets_simple.go: rowCache**
  - Кэш строк никогда не инвалидируется
  - Добавить периодическую очистку

### 5.3.3 Оптимизация Google Sheets
- [ ] **FindBookingRow() читает весь столбец A**
  - Проблема: При большом количестве строк — медленно
  - Решение: Держать кэш row_id -> row_number в памяти/Redis

---

## 5.4 Безопасность (средний приоритет)

### 5.4.1 Валидация входных данных
- [x] **internal/bot/handler.go: handleDateInput**
  - Нет проверки максимальной даты бронирования
  - Можно бронировать на годы вперед
- [x] **internal/bot/manager.go: handleManagerClientName**
  - Нет санитизации имени клиента
- [x] **internal/api/http_server.go: handleAvailability**
  - Имя товара из URL не санитизируется

### 5.4.2 Конфигурация
- [x] **Переместить API ключи в переменные окружения**
  - Сейчас: хранятся в plain text в config.yaml
  - Решение: Использовать ${API_KEY} синтаксис

### 5.4.3 Защита от DDoS/спама
- [ ] **Добавить rate limiting для обычных пользователей**
  - Сейчас есть только для API
  - Добавить лимит сообщений/минуту для бота

### 5.4.4 Аудит-логи
- [ ] **Логировать security-события с контекстом**
  - Неуспешные попытки авторизации
  - Действия менеджеров (confirm/reject)
  - Rate limit violations

---

## 5.5 Тестирование (высокий приоритет)

### 5.5.1 Отсутствующие unit-тесты
- [ ] **internal/bot/** — 0% покрытия
  - Критичные функции: handleBooking, finalizeBooking, confirm/reject
- [ ] **internal/config/** — 0% покрытия
  - Тесты загрузки конфига, валидации
- [x] **internal/database/** — 0% покрытия (bronivik_jr)
  - CRUD операции, транзакции
- [ ] **internal/google/** — 0% покрытия
  - Mock Google API для тестов
- [ ] **internal/events/** — 0% покрытия
  - EventBus подписки и публикация

### 5.5.2 Интеграционные тесты
- [ ] **Полный flow бронирования**
  - create → confirm → complete
  - create → reject
  - create → cancel
- [ ] **Конкурентный доступ**
  - Одновременное бронирование одного слота
- [ ] **Failover Redis**
  - Переключение на in-memory при недоступности Redis

### 5.5.3 Инфраструктура тестирования
- [ ] **Добавить в Makefile**
  ```makefile
  test:
      go test -v -race ./...
  
  test-coverage:
      go test -coverprofile=coverage.out ./...
      go tool cover -html=coverage.out
  
  lint:
      golangci-lint run
  ```

---

## 5.6 Архитектура (средний приоритет)

### 5.6.1 Dependency Injection [DONE]
- [x] **internal/bot/handler.go: Bot struct**
  - Зависит напрямую от 6 конкретных типов
  - Решение: Ввести интерфейсы для зависимостей
  ```go
  type Bot struct {
      sender      TelegramSender
      repo        BookingRepository
      sheets      SheetsWriter
      eventBus    EventPublisher
      // ...
  }
  ```

### 5.6.2 Service Layer [DONE]
- [x] **Извлечь бизнес-логику из bot package**
  - Создать internal/service/booking_service.go
  - Bot должен только обрабатывать Telegram API

### 5.6.3 Интерфейсы [DONE]
- [x] **internal/database/database.go**
  - Создать `BookingRepository` interface
  - Упростит мокирование для тестов
- [x] **internal/bot/handler.go**
  - Создать `TelegramSender` interface

---

## 5.7 Конфигурация (средний приоритет)

### 5.7.1 Валидация конфига [DONE]
- [x] **internal/config/config.go: добавить Validate()**

### 5.7.2 Конфигурируемые параметры
- [ ] **Вынести в конфиг**
  - Время отправки напоминаний (сейчас 09:00)
  - Размер пагинации
  - Максимальная дата бронирования вперед
  - Минимальное время до бронирования

---

## 5.8 Мониторинг и Health Checks (средний приоритет)

### 5.8.1 Health checks для bronivik_jr
- [x] **Добавить /healthz и /readyz для бота**
  - Проверка БД
  - Проверка Google Sheets connectivity
  - Проверка Redis (если используется)

### 5.8.2 Дополнительные метрики
- [ ] **Добавить метрики**
  - `booking_create_duration_seconds` — время создания бронирования
  - `bot_message_processing_duration_seconds`
  - `database_query_duration_seconds`
  - `sheets_sync_success_total` / `sheets_sync_failure_total`

### 5.8.3 Алертинг
- [ ] **Создать правила алертинга**
  - База данных недоступна > 1 минуты
  - Google Sheets sync failures > 5 подряд
  - Rate limit превышен > 100 раз/минуту

---

## 5.9 DevOps (средний приоритет)

### 5.9.1 CI/CD
- [ ] **Создать .github/workflows/ci.yml**
  ```yaml
  name: CI
  on: [push, pull_request]
  jobs:
    test:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - uses: actions/setup-go@v5
          with:
            go-version: '1.21'
        - run: go test -race ./...
        - run: golangci-lint run
  ```

### 5.9.2 Линтер
- [ ] **Добавить .golangci.yml**
  - errcheck, govet, staticcheck, gosec

### 5.9.3 Dockerfile
- [x] **Добавить HEALTHCHECK**
  ```dockerfile
  HEALTHCHECK --interval=30s --timeout=3s \
    CMD wget --spider http://localhost:8081/healthz || exit 1
  ```
- [ ] **Добавить .dockerignore**
  - .git, .env, *.md, tests/

### 5.9.4 Бэкапы
- [ ] **Реализовать автобэкап SQLite**
  - Конфиг есть (backup.schedule), но нет реализации
  - Добавить cron-job или goroutine для бэкапа

---

## 5.10 Документация (низкий приоритет)

### 5.10.1 Godoc комментарии
- [ ] **Добавить документацию к публичным методам**
  - internal/bot/handler.go
  - internal/database/database.go
  - internal/api/*.go

### 5.10.2 README
- [ ] **Добавить в README.md**
  - Архитектурная диаграмма
  - API документация (endpoints, примеры)
  - Deployment guide
- [ ] **Создать bronivik_crm/README.md**

### 5.10.3 API документация
- [ ] **Добавить OpenAPI/Swagger spec**
  - Для HTTP endpoints (/api/v1/*)
- [ ] **Обновить proto файлы с комментариями**
  - Описание методов и полей

---

## 5.11 Схема базы данных (низкий приоритет)

### 5.11.1 Целостность данных
- [x] **Добавить foreign key constraints**
  - bookings.item_id → items.id
  - bookings.user_id → users.telegram_id
- [x] **Добавить unique constraint**
  - Предотвратить двойное бронирование (item_id, date, status='confirmed')

### 5.11.2 Индексы
- [x] **Проверить и добавить индексы**
  - bookings(date, item_id, status) — для availability queries
  - sync_queue(next_retry_at, status) — для worker

---

# 📊 ПРИОРИТЕТЫ ИСПРАВЛЕНИЙ

## Критические (исправить в течение недели)
1. [x] Race condition в userStates (mutex)
2. [ ] Дубликаты ID в items.yaml
3. [x] Unit-тесты для database package
4. [x] Валидация конфигурации

## Высокий приоритет (2-3 недели)
5. [x] Сохранение userStates в Redis
6. [x] Рефакторинг длинных функций
7. [ ] Вынести константы статусов
8. [ ] CI/CD pipeline с линтером
9. [x] Батч-запросы для availability

## Средний приоритет (1-2 месяца)
10. [ ] Service layer для бизнес-логики
11. [ ] Health checks для всех компонентов
12. [ ] Дополнительные метрики
13. [ ] Интеграционные тесты
14. [ ] API документация

## Низкий приоритет (ongoing)
15. [ ] Godoc комментарии
16. [ ] Docker оптимизации
17. [ ] Бэкап стратегия
18. [ ] README улучшения